name: aqua_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_map: ^6.2.1
  latlong2: ^0.9.1

  #Firebase Core
  firebase_core: ^3.6.0

  #Authentication
  firebase_auth: ^5.3.0

  #Firestore (for water parameters)
  cloud_firestore: ^5.4.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
